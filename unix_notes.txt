~$ grep -n "string" file.txt
        // search file.txt for instances of "string" and print it out with line 
        // numbers (-n)
        
shift - cmd - left/right
        // switch between multiple tabs in terminal
